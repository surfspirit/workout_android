package ru.workout24.utills

/*
fun View.showSnackbar(message: String?, color: Int) {
    val snakbar = Snackbar.make(this, message ?: "", Snackbar.LENGTH_LONG)
    snakbar.view.setBackgroundColor(color)
    snakbar.show()
}

fun View.showSnackbar(message: String?, color: Int, buttonText:String, callback:(View)-> Unit) {
    val snakbar = Snackbar.make(this, message ?: "", Snackbar.LENGTH_LONG)
    snakbar.view.setBackgroundColor(color)
    snakbar.setAction(buttonText, callback)
    snakbar.show()
}
*/