<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/white"
    tools:context=".ui.wg_bonuses.WgBonusesFragment">

    <ru.workout24.utills.custom_views.CustomToolbar
        android:id="@+id/appBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/black"
        app:title="@string/leaders_list" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_bonuses"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/appBar"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"/>

    <ru.workout24.utills.custom_views.CustomButton
        android:id="@+id/show_mine_pos"
        android:layout_width="135dp"
        android:layout_height="48dp"
        app:isRounded="true"
        android:background="@drawable/custom_btn_ripple"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="28dp"
        android:layout_marginBottom="40dp" />

</RelativeLayout>